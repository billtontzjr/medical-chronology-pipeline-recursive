# Workspace acceptance record

September 12, 2026. This record describes tested software behavior, not certification of a medical chronology.

## Completed locally

- Full repository suite: **382 passed, 4 credential-dependent tests skipped** after live-acceptance validation fixes. The suite uses fictional cases and controlled provider/Dropbox adapters.
- Authenticated gateway/API acceptance includes new-case creation, repeated Resume producing one job, protected PDF delivery, foreign-origin and missing-header rejection, login return routing, historical links, pause, archive and restore.
- In-app browser acceptance at desktop (1440px) and narrow (390px) widths confirmed original physical-page images, OCR/citation navigation, duplicate side-by-side comparison, correct source/page selection when opening review decisions, and reachable review actions. The fictional preview correctly rejects saving review changes. A Word download was received through the browser and matched the saved export byte for byte. The native embedded PDF viewer was blank in this browser; it was replaced with authenticated Poppler page images plus links to enlarge the page or open the original. Tests cover actual first/second page content, authentication, cross-case isolation, invalid pages, changed-source rejection and unchanged originals.
- Source-backed parser acceptance used three existing clinical PDFs comprising 14 physical pages. All 13 files in the retained source inventory matched their saved hashes. The checks confirmed patient association, explicit service-date roles and preserved page references; wrong-patient and DOB-as-service-date variants were rejected. No originals were modified or sent to a new service. No clinical summary was generated by that offline parser check.
- Source comparison exposed surname-first middle-name and inline/wrapped DOS header cases. Regression tests now cover those shapes without committing patient data. Other new tests cover multiple labeled therapy dates on one page, same-day procedure consolidation with all citations, exact file aliases, source-version invalidation, stale review decisions, immutable exports, incomplete delivery, optional companion failures and preflight detection of active work.
- All five supplied Word references were inspected structurally. Four share a centered, bold, underlined heading with justified narrative; the other uses a plain left-aligned heading/body. The exporter offers those two patterns plus the existing classic profile. Synthetic short/long encounter and grouped-attendance fixtures were rendered for all three profiles, with every page inspected. Four fictional preview Word downloads were also rendered and inspected. Page numbers, margins, narrative content and variable paragraph length were retained, without tables or a Visit Type label.
- New storage is separate from historical session directories. Schema-downgrade rejection, collision preservation, archive/restore, stale-lease recovery, a live case-lock conflict and case-scoped path traversal protections were exercised.

## Opt-in production acceptance

PR #24 was deployed with default workspace routing disabled and automatic deployments off. The original active-run files and checkpoints were backed up and their 455 file hashes verified unchanged after deployment. No active case locks or queued/running workspace jobs were present at the deployment check. The authenticated workspace route is live; the browser is awaiting team sign-in.

Two isolated cases reached export/upload completion through the configured production services. This is not a clinical acceptance pass: a fictional case exposed false review flags for a discrete therapy-date list and a same-day merge header, while the constrained three-report benchmark exposed date-header and citation presentation mismatches. Uncertain entries were withheld and responses/checkpoints preserved.

The follow-up fixes recognize explicit discrete attendance lists, corroborated EMR DOS/appointment/progress-note dates, Oxford commas and alphabetic hyphen line breaks. Accepted citations retain the actual original source span. Changed words, doses, levels, laterality, dates and wrong-page citations remain rejected. Same-day merge header syntax can receive one structural correction; changed dates/providers remain substantive review failures. A new evidence protocol version preserves previous results instead of silently replaying them under changed rules.

The PR #25 fictional rerun produced the expected three entries and all five service dates, with the same-day evaluation/procedure combined. Only the intentionally wrong-patient source remains flagged. Exports retained exact physical-page citations, excluded the legal/deposition fixtures, and contained no Word tables or Visit Type label. The upload phase completed. Production Word rendering and authenticated browser review still require download/sign-in access.

Read-only replay of the existing benchmark responses then exposed a composite service heading (a note heading plus a source service description). The next patch accepts this display composition only when both complete parts occur on the same page; invented words and cross-page combinations remain rejected, and the independent clinical audit remains mandatory. All three retained benchmark candidates passed initial validation in a read-only diagnostic using their exact source service descriptions. This did not approve the candidates, regenerate them, or call a provider.

## Still required before broad rollout

1. Deploy the service-heading follow-up only after another worker/lock check. Complete actual Word rendering/download review; retain the successful PR #25 fictional acceptance as a software regression benchmark.
2. Obtain explicit approval for the identified provider and benchmark inputs before any additional real-record transmission. Inspect generated clinical content against original records; successful job or upload status does not satisfy this gate.
3. Complete authenticated Chrome workflow acceptance after team sign-in. The read-only fictional local preview does not establish production behavior.
4. Enable default workspace routing only after those acceptance gates pass. Preserve the historical run and archived cases.
